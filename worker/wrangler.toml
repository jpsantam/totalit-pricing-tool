name = "totalit-master-costs"
main = "master-costs.js"
compatibility_date = "2026-07-10"

# Created once via: wrangler kv namespace create MASTER_COSTS
# Paste the returned id below.
kv_namespaces = [
  { binding = "MASTER_COSTS", id = "REPLACE_WITH_KV_NAMESPACE_ID" }
]

# Set separately (not committed): wrangler secret put MASTER_PASSWORD
